<style>
 @import url(https://fonts.googleapis.com/css?family=Lato:400,700);

.login_bar {
  display: flex;
  width: 280px;
  height: 40px;
  background: #F7F7F7;
  border-radius: 10px;
  top: 50%;
  position: relative;
  z-index: 0;
  overflow: hidden;
  margin: 0 auto;
}

.login {
  background: transparent;
  position: absolute;
  height: 100%;
  width: 180px;
  padding-left: 10px;
  text-align: left;
  font-size: 12px;
  border: none;
  line-height: 40px;
  vertical-align: middle;
  z-index: 10;
  -webkit-transition: transform 0.2s ease-in-out 0s;
     -moz-transition: transform 0.2s ease-in-out 0s;
       -o-transition: transform 0.2s ease-in-out 0s;
          transition: transform 0.2s ease-in-out 0s;
}

.login_active {
  -webkit-transform: translateX(280px);
     -moz-transform: translateX(280px);
      -ms-transform: translateX(280px);
       -o-transform: translateX(280px);
          transform: translateX(280px);
}

.subscribe_button:hover{
  background-color: #0091EA;
  /*#282828;*/
  cursor:pointer;
}

.subscribe_button {
  background: #1CA9FF;
  /*#383838;*/

  position: absolute;
  top: -40px;
  left: 190px;
  border-top-right-radius: 10px;
  border-bottom-right-radius: 10px;
  height: 120px;
  width: 91px;
  text-align: center;
  font-size: 12px;
  font-weight: 500;
  line-height: 100px;
  vertical-align: middle;
  color: white;
  -webkit-transition: transform 0.5s ease 0s;
     -moz-transition: transform 0.5s ease 0s;
       -o-transition: transform 0.5s ease 0s;
          transition: transform 0.5s ease 0s;
}

.subscribe_button_active{
  -webkit-transform: translateY(40px);
     -moz-transform: translateY(40px);
      -ms-transform: translateY(40px);
       -o-transform: translateY(40px);
          transform: translateY(40px);
}

.subscribing{
  background: #CDD4DC;
  height: 100%;
  width: 280px;
  z-index: 5;
  position: absolute;
  left:-280px;
  display: inline-block;
  -webkit-transition: transform 1s ease 0s ;
     -moz-transition: transform 1s ease 0s ;
       -o-transition: transform 1s ease 0s ;
          transition: transform 1s ease 0s ;
}

.subscribing_active {
  -webkit-transform: translateX(280px);
     -moz-transform: translateX(280px);
      -ms-transform: translateX(280px);
       -o-transform: translateX(280px);
          transform: translateX(280px);
}

.thanks{
  background: #FFCE00;
  height: 100%;
  width: 280px;
  z-index: 5;
  text-align: left;
  line-height: 40px;
  vertical-align: middle;
  padding-left: 10px;
  position: absolute;
  font-size: 12px;
  top:40px;
  display: inline-block;
  -webkit-transition: transform 0.5s ease 0s ;
     -moz-transition: transform 0.5s ease 0s ;
       -o-transition: transform 0.5s ease 0s ;
          transition: transform 0.5s ease 0s ;
}

.thanks_active {
  -webkit-transform: translateY(-40px);
     -moz-transform: translateY(-40px);
      -ms-transform: translateY(-40px);
       -o-transform: translateY(-40px);
          transform: translateY(-40px);
}

</style>

<p class="sub">Be the first to know <span>&</span> get Kilometers</p>
<div class="login_bar">
  <%= form_for @user, url: users_email_verification_path, method: :post do |f| %>
    <%= f.text_field :email, class: "login", :placeholder => "Type in your email" %>
    <input class="submit subscribe_button" onclick="subscribeOnClick()" name="commit" type="submit" value="Step Inside" />
  <% end %>
  <span class="subscribing"></span>
  <span class="thanks">Thank you. Verify your email to get early access.</span>
                                                                        <p></p>
</div>

<script>
var subscribe_button = document.querySelector(".subscribe_button");

subscribe_button.addEventListener('click', function(){
  var subscribing = document.querySelector(".subscribing");
  var thanks = document.querySelector(".thanks");
  var login = document.querySelector(".login");

  subscribing.classList.add("subscribing_active");
  subscribe_button.classList.add("subscribe_button_active");
  setTimeout(function(){
    login.classList.add("login_active");
  }, 1200);
  setTimeout(function(){
    thanks.classList.add("thanks_active");
  }, 1400);

  setTimeout(function(){
    thanks.classList.remove("thanks_active");
    login.classList.remove("login_active");
    subscribing.classList.remove("subscribing_active");
    subscribe_button.classList.remove("subscribe_button_active");
  }, 4000000);
});
</script>
